% @WELLOBJ
%
% Files
%   plotXS    - gr.plotXS(ax,iy,well,varargin)  plot wells on X-section.
%   plotYS    - gr.plotYS(ax,ix,well,varargin)  plot wells on Y-section.
%   PNTSRC    - [WEL, PNTSRC] = well.PNTSRC() --- generate a PNTSRC array for MODFLOW
%   set2meanQ - well=well.set2meanQ --- set Q to mean of all non NaN and non zero values
%   setCout   - well=well.setCout(C,iComp)  % adds Cout extracted by the sceen to
%   setTime   - well=well.setTime(time) --- add values for t and Dt to wells
%   setWell   - well = well.setWell(gr,HK) plces the well in the grid
%   WEL       - = well.WEL() --- generate a WEL array for MODFLOW
%   wellObj   - 
